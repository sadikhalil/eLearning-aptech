// Thin re-export of AuthContext for cleaner imports
// Usage: import { useAuth } from "../hooks/useAuth"
// instead of: import { useAuth } from "../context/AuthContext"
export { useAuth } from "../context/AuthContext";
