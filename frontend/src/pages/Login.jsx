// Standalone page — router points here
// Form logic lives in components/auth/LoginForm.jsx
import LoginForm from "../components/auth/LoginForm";

const Login = () => <LoginForm />;

export default Login;
