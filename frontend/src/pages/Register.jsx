// Standalone page — router points here
// Form logic lives in components/auth/RegisterForm.jsx
import RegisterForm from "../components/auth/RegisterForm";

const Register = () => <RegisterForm />;

export default Register;
